DATABASE_URL=postgres://postgres:postgres@localhost:5432/mod_compliance
JWT_SECRET=change-me-in-production
PORT=3001
UPLOAD_DIR=./uploads

# Required for fact extraction (on document upload) and the Answer Engine
# (on questionnaire runs). Without it, both fail cleanly instead of crashing
# the server. Free tier — get a key with no payment method needed at
# https://aistudio.google.com/apikey
GEMINI_API_KEY=
