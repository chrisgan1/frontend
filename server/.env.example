DATABASE_URL=postgres://postgres:postgres@localhost:5432/mod_compliance
JWT_SECRET=change-me-in-production
PORT=3001
UPLOAD_DIR=./uploads

# Required for "Draft from evidence (AI)" on request items. Without it, that
# feature returns a clear error instead of crashing the server.
ANTHROPIC_API_KEY=
